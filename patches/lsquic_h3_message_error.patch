diff --git a/src/liblsquic/lsquic_qdec_hdl.h b/src/liblsquic/lsquic_qdec_hdl.h
index 4f9cfeb..1058193 100644
--- a/src/liblsquic/lsquic_qdec_hdl.h
+++ b/src/liblsquic/lsquic_qdec_hdl.h
@@ -25,6 +25,7 @@ struct qpack_dec_hdl
         QDH_CONN_ABORTED    = 1 << 1,
         QDH_SAVE_UA         = 1 << 2,
         QDH_SERVER          = 1 << 3,
+        QDH_HSI_HEADER_ERROR = 1 << 4,
     }                        qdh_flags;
     struct lsqpack_dec       qdh_decoder;
     struct lsquic_stream    *qdh_enc_sm_in;
@@ -70,6 +71,9 @@ lsquic_qdh_header_in_begin (struct qpack_dec_hdl *, struct lsquic_stream *,
 enum lsqpack_read_header_status
 lsquic_qdh_header_in_continue (struct qpack_dec_hdl *, struct lsquic_stream *,
                                 const unsigned char **, size_t);
+
+int
+lsquic_qdh_take_hsi_error (struct qpack_dec_hdl *);
 
 void
 lsquic_qdh_cancel_stream (struct qpack_dec_hdl *,
diff --git a/src/liblsquic/lsquic_qdec_hdl.c b/src/liblsquic/lsquic_qdec_hdl.c
index 46dc3d6..fd3a06c 100644
--- a/src/liblsquic/lsquic_qdec_hdl.c
+++ b/src/liblsquic/lsquic_qdec_hdl.c
@@ -582,6 +582,8 @@ qdh_hsi_process_wrapper (struct qpack_dec_hdl *qdh, void *hset,
 
     retval = qdh->qdh_enpub->enp_hsi_if->hsi_process_header(hset, xhdr);
-    if (0 != retval)
+    if (retval > 0)
+        qdh->qdh_flags |= QDH_HSI_HEADER_ERROR;
+    else if (retval < 0)
         QDH_ABORT_CONN_ONCE(qdh, 1, HEC_MESSAGE_ERROR,
                                             "error processing headers");
 
@@ -604,11 +606,9 @@ qdh_process_header (void *stream_p, struct lsxpack_header *xhdr)
         if (u->ctx.cont_len.has < 0)
         {
-            QDH_ABORT_CONN_ONCE(qdh, 1,
-                HEC_MESSAGE_ERROR, "invalid or ambiguous content-length");
+            qdh->qdh_flags |= QDH_HSI_HEADER_ERROR;
             return 1;
         }
-        if (u->ctx.cont_len.has > 0)
-            (void) lsquic_stream_verify_len(stream, u->ctx.cont_len.value);
+        /* The application enforces body length with a stream-scoped error. */
     }
     else if ((qdh->qdh_flags & QDH_SERVER) &&
             /* If PRIORITY_UPDATE has been used (SMBF_HPRIO_SET), then the
@@ -658,6 +658,23 @@ qdh_maybe_destroy_hblock_ctx (struct qpack_dec_hdl *qdh,
 }
 
 
+static void
+qdh_reset_header_error_stream (struct qpack_dec_hdl *qdh,
+                                                struct lsquic_stream *stream)
+{
+    lsquic_stream_maybe_reset(stream, HEC_MESSAGE_ERROR, 1);
+}
+
+
+int
+lsquic_qdh_take_hsi_error (struct qpack_dec_hdl *qdh)
+{
+    const int had_error = !!(qdh->qdh_flags & QDH_HSI_HEADER_ERROR);
+    qdh->qdh_flags &= ~QDH_HSI_HEADER_ERROR;
+    return had_error;
+}
+
+
 static enum lsqpack_read_header_status
 qdh_header_read_results (struct qpack_dec_hdl *qdh,
         struct lsquic_stream *stream, enum lsqpack_read_header_status rhs,
@@ -698,7 +715,9 @@ qdh_header_read_results (struct qpack_dec_hdl *qdh,
             {
                 LSQ_DEBUG("finishing hset failed");
                 free(uh);
                 qdh->qdh_enpub->enp_hsi_if->hsi_discard_header_set(hset);
+                if (qdh->qdh_flags & QDH_HSI_HEADER_ERROR)
+                    qdh_reset_header_error_stream(qdh, stream);
                 return LQRHS_ERROR;
             }
             uh->uh_hset = hset;
@@ -727,6 +746,11 @@ qdh_header_read_results (struct qpack_dec_hdl *qdh,
     }
     else if (rhs == LQRHS_ERROR)
     {
+        if (qdh->qdh_flags & QDH_HSI_HEADER_ERROR)
+        {
+            qdh_reset_header_error_stream(qdh, stream);
+            return rhs;
+        }
         qdh_maybe_destroy_hblock_ctx(qdh, stream);
         qerr = lsqpack_dec_get_err_info(&qdh->qdh_decoder);
         QDH_ABORT_CONN_ONCE(qdh, 1,
diff --git a/src/liblsquic/lsquic_stream.c b/src/liblsquic/lsquic_stream.c
index 0d76975..a52623b 100644
--- a/src/liblsquic/lsquic_stream.c
+++ b/src/liblsquic/lsquic_stream.c
@@ -5060,7 +5060,14 @@ hq_read (void *context, const unsigned char *buf, size_t len, int fin)
                 default:
                     assert(LQRHS_ERROR == rhs);
                     stream->sm_qflags &= ~SMQF_QPACK_DEC;
                     filter->hqfi_flags |= HQFI_FLAG_ERROR;
+                    if (lsquic_qdh_take_hsi_error(
+                                        stream->conn_pub->u.ietf.qdh))
+                    {
+                        filter->hqfi_left = 0;
+                        filter->hqfi_state = HQFI_STATE_FRAME_HEADER_BEGIN;
+                        goto end;
+                    }
                     LSQ_INFO("error processing header block");
                     abort_connection(stream);   /* XXX Overkill? */
                     goto end;
